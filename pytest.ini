[pytest]
# The code lives in folders at the repository root (ingest/, pricing/, publish/,
# dashboard/), so tests import it the same way the scripts and dbt do.
pythonpath = .
testpaths = tests/python
addopts = -q
