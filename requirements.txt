# Ingest
requests>=2.31
pandas>=2.0
pyarrow>=15.0
python-dotenv>=1.0

# Warehouse and transformation
duckdb>=1.1
dbt-duckdb>=1.11,<2

# Hedonic model (pricing/hedonic.py). statsmodels rather than scikit-learn: the model is read
# for its coefficients, which need standard errors, and scikit-learn reports none.
numpy>=1.26
statsmodels>=0.14

# Serving
streamlit>=1.30
altair>=5.0

# Tests
pytest>=8.0
